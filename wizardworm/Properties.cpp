#include "stdafx.h"
#include "Properties.h"
#include <SFML/Graphics.hpp>
/*


Properties::Properties(sf::RenderWindow *w)
{
	value = 50; // Kezdeti érték
	position.set_x(10);
	position.set_y(10);
	window = w;
}

Properties::Properties(){}

Properties::~Properties(){}



void Properties::set_position(int x, int y) {
	position.set_x(x);
	position.set_y(y);
}

void Properties::move(int x, int y) {
	position.incr_x(x);
	position.incr_y(y);
	
	//position.set_x(position.get_x() + x);
	//position.set_y(position.get_y() + y);
}



pos Properties::get_position() {
	return position;
}

void Properties::set_value(int val) {
	value = val;
}

int Properties::get_value() {
	return value;
}

void Properties::draw(sf::Color color) {
	sf::RectangleShape rectangle;
	rectangle.setSize(sf::Vector2f(value, 5));
	rectangle.setFillColor(color);
	rectangle.setPosition(position.get_x(), position.get_y());
	window->draw(rectangle);
}



*/